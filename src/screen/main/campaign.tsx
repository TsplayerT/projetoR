import React from 'react';

import { Screen, MenuItem } from '../../styles/basics';

export const CampaignScreen: React.FC = () => (
  <Screen>
    <MenuItem>Campaign Screen</MenuItem>
  </Screen>
);
