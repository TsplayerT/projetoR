import React from 'react';

import { Screen } from '../../../components/Screen/index';
import { MenuTitle } from '../../../styles/basics';

export const TheaterScreen: React.FC = () => (
  <Screen>
    <MenuTitle>Theater Screen</MenuTitle>
  </Screen>
);