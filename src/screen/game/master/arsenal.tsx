import React from 'react';

import { Screen, MenuTitle } from '../../../styles/basics';

export const ArsenalScreen: React.FC = () => (
  <Screen>
    <MenuTitle>Arsenal Screen</MenuTitle>
  </Screen>
);